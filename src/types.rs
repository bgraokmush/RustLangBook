fn types() {
    // Integers
    let a = 99_222; // 99,222 (decimal)
    let b = 0xff; // 255 (hex)
    let c = 0o77; // 63 (octal)
    let d = 0b1111_0000; // 240 (binary)

    let f: u8 = 255; // 0 to 255
    let g: i8 = -128; // -128 to 127

    // Floating-point numbers
    let h = 2.0; // f64
    let i: f32 = 3.0; // f32

    // Booleans
    let j = true;
    let k: bool = false;

    // Characters
    let l = 'z';
    let m = 'ℤ';
    let n = '😻';
}
